# Simulation/build artifacts
build/
regress/results/

# Third-party IP tarballs (scripts/fetch_ip.sh)
ip/third_party/
tests/core/asm/work/

# Synopsys/Verdi generated files
verdiLog/
novas.conf
sim/filelists/verdiLog/
sim/filelists/novas.conf
*.fsdb
*.vcd
*.vpd
*.key
ucli.key
simv
simv.daidir/
csrc/

# Design Compiler generated artifacts
dc/work/
dc/outputs/
dc/reports/
dc/logs/
dc/alib-*/
dc/command.log
dc/filenames.log
dc/default.svf

# Tool and editor scratch notes
codex_txt
cursor_txt

# Local AI / status context (not for GitHub)
AI_DEV_CONTEXT.local.md

# Last run_case.sh build directory marker (see scripts/open_rv.sh, shell function t)
.open_rv32m_last_case_build_dir

# Per-machine path overrides (see scripts/open_rv.local.sh.example)
scripts/open_rv.local.sh

# Large reference docs (use spec/RV32IM_Current_Implementation.md in git)
spec/*.pdf
spec/*.docx

# OS/editor noise
.DS_Store
*~
*.swp

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# Verilator / C++ build debris (if ever outside build/)
obj_dir/

# IDEs
.idea/
.vscode/
